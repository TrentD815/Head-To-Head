export interface PlayerProfile {
  profilePicSource ?: string;
  name ?: string;
  team ?: string;
  teamLogoSource ?: string;
  weight ?: number | string;
  height ?: string;
  position ?: string;
  number ?: number;
}
