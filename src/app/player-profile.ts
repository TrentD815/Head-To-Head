export interface PlayerProfile {
  profilePicSource ?: string;
  name ?: string;
  team ?: string;
  teamLogoSource ?: string;
  weight ?: number;
  height ?: string;
  position ?: string;
  number ?: number;
}
